---
profile: production

exclude_paths:
  - changelogs/changelog.yaml
  - roles/restore/tasks/health_checks/templates
  - tests/
skip_list:
  # this rule error out correct vars names for network validated content.
  - var-naming[no-role-prefix]
